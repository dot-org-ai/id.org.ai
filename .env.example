# E2E Test Environment Variables for id.org.ai / oauth.do
#
# Copy to .env and fill in values:
#   cp .env.example .env

# Required — email+password and magic link login tests
# The test user (e2e-auth-test@emails.do) must already exist in WorkOS.
# Create it via AuthKit sign-up or the WorkOS dashboard.
E2E_AUTH_PASSWORD=

# Optional — GitHub OAuth tests (skip if not set)
E2E_GITHUB_EMAIL=
E2E_GITHUB_PASSWORD=

# Optional — Google OAuth tests (skip if not set)
E2E_GOOGLE_EMAIL=
E2E_GOOGLE_PASSWORD=

# Override test target URL (defaults to https://oauth.do)
# ID_URL=https://oauth.do
