// src/services/auth/errors.ts
// Auth/Session errors reuse foundation AuthError — no domain-specific errors needed yet.
// This file exists for forward compatibility (e.g., SessionExpiredError, ConcurrentSessionError).
export {}
