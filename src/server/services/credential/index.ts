/**
 * Credential fact service — held-credential facts per principal
 * (beads id-j0k, ADR 0016 C6).
 */

export type { CredentialFactService, CredentialVerificationEvent, HeldCredentialFact } from './types'
export { CredentialFactServiceImpl } from './service'
